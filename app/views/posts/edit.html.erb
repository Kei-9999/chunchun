<% if @post.errors.any? %>
  <%= @post.errors.count %>errors prohibited this user from
  <% @post.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

　<div class="container">
    <div class="row">
     <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

　　　<h1>羽繕い</h1>

　　　　<%= form_with model:@post, url:"/posts/#{@post.id}", local:true do |f| %>

  　　　<div class="form-group">

           <%= f.label:title,"しゃしん" %>
           <br>
           <%=f.attachment_field :post_image %>

 　 　　</div>
 　 　　
        <div class="form-group">
         <%= f.label:body,"エピソード" %>
         <br>
         <%= f.text_area :body,class:"form-control" %>
      </div>
         <br>
           <%= f.submit '羽繕い完了',class:"btn btn-success" %>

           <br>
           <%= link_to "文鳥画像", post_path(@post.id)%> | <%= link_to "もどる", posts_path %></p>
        <% end %>
      </div>
    </div>
  </div>