<% if @post.errors.any? %>
  <%= @post.errors.count %>errors prohibited this user from
  <% @post.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

　<div class="container">
    <div class="row">
     <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

　　　<h1>Editing posts</h1>

　　　　<%= form_with model:@post, url:"/posts/#{@post.id}", local:true do |f| %>

  　　　<div class="form-group">

           <%= f.label:title,"Photo" %>
           <br>
           <%=f.attachment_field :post_image %>

 　 　　</div>
 　 　　
        <div class="form-group">
         <%= f.label:body,"Column" %>
         <br>
         <%= f.text_area :body,class:"form-control" %>
      </div>
         <br>
           <%= f.submit 'Update Post',class:"btn btn-success" %>

           <br>
           <%= link_to "Show", post_path(@post.id)%> | <%= link_to "Back", posts_path %></p>
        <% end %>
      </div>
    </div>
  </div>